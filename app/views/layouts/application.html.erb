<!DOCTYPE html>
<html>
  <head>
    <title>DataProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <section class="section">
      <div class="container">
        <nav class="navbar" role="navigation" aria-label="main navigation">
          <div class="navbar-brand">
            <figure class="navbar-item">
              <img src=<%= asset_path("logo.png") %> alt="MTG Logo" width="112" height="33">
            </figure>
          </div>

          <div id="navbarBasicExample" class="navbar-menu">
            <div class="navbar-start">
              <%= link_to "Home", home_index_path, class: "navbar-item" %>

              <%= link_to "About", about_index_path, class: "navbar-item" %>

              <%= link_to "Contact", contact_index_path, class: "navbar-item" %>

              <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link">
                  More
                </a>

                <div class="navbar-dropdown">
                  <%= link_to "Cards", cards_index_path, class: "navbar-item" %>
                  <%= link_to "Sets", magic_sets_index_path, class: "navbar-item" %>
                  <%= link_to "Artists", artists_index_path, class: "navbar-item" %>
                </div>
              </div>
            </div>
            <div class="navbar-end">
              <% if controller_name != "checkout" %>
                <%= render "layouts/cart" %>
                <% if @cart_id.count > 0 %>
                  <%= button_to 'Checkout', checkout_index_path, method: :get, class: "button is-primary"%>
                <% end %>
              <% end %>
            </div>
        </nav>
      </div>
    </section>
  </header>

  <body>
    <section class="section">
      <div class="container">
        <main class="container mx-auto mt-28 px-5 flex">
          <%= form_with(url: search_cards_path, method: :get, local: true) do %>

            <div class="field is-ground mb-5 is-display-flex flex-direction-row">
              <div class="select is-info">
                <%= select_tag 'type', options_for_select(Type.all.map{|el| [el.name, el.id]}), :prompt => "All Card Types", class: "select"%>
              </div>
              <%= text_field_tag(:keywords, nil, class: "input", placeholder: 'Search for cards!') %>
              <%= submit_tag("Search", class: "button is-primary")%>
            </div>
          <% end %>
          <%= yield %>
        </main>
      </div>
    </section>
  </body>

  <footer class="footer">
    <section class="section">
      <div class="container">
        <% if controller_name == 'cards' %>
          <p>Card data was provided by <a href="https://mtgjson.com/">MTG JSON</a></p>
        <% elsif controller_name == 'magic_sets' %>
          <p>Set data was provided by <a href="https://mtgjson.com/">MTG JSON</a> and <a href="https://scryfall.com/docs/api">Scryfall API</a></p>
        <% end %>
      </div>
    </section>
  </footer>

</html>
